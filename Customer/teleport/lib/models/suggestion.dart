class Suggestion{
  final uid;
  final shopUID;
  final title;
  final subtitle;
  final type;
  final item;
  final shop;
  Suggestion({this.uid, this.shopUID, this.title, this.subtitle, this.type, this.item, this.shop});
}